#include <stdio.h>

int main(){
    int i = 1, n, p = 1;
    printf("digite um numero: ");
    scanf("%d", &n);
    
    do{
        i *= (p+1);
        p += 1;
    }while(p <= n-1);
    
    printf("%d\n", i);
        
    return 0;
}
    
    
